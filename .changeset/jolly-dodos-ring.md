---
"@langchain/community": minor
---

Remove broken ChatMinimax chat model. Minimax removed old API endpoints and migrated to Anthropic-compatible and OpenAI-compatible APIs. Use `@langchain/anthropic` or `@langchain/openai` with Minimax's API URLs instead.
